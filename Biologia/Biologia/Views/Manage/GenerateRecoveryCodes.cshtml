@{
    ViewData["Title"] = "Generate two-factor authentication (2FA) recovery codes";
    ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Esta acción genera nuevos códigos de recuperación.</strong>
    </p>
    <p>
        Si pierde su dispositivo y no tiene los códigos de recuperación, perderá el acceso a su cuenta.
    </p>
    <p>
        La generación de nuevos códigos de recuperación no cambia las claves utilizadas en las aplicaciones de autenticación. 
        Si desea cambiar la clave utilizada en una aplicación de autenticación, debería <a asp-action="ResetAuthenticatorWarning">restablecer sus claves de autenticador.</a>
    </p>
</div>

<div>
    <form asp-action="GenerateRecoveryCodes" method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Generar códigos de recuperación</button>
    </form>
</div>
